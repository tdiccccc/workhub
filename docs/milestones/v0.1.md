# Milestone v0.1

## ゴール

認証とProject管理まで完成させる。

---

## 機能

- ログイン
- ログアウト
- ログイン中ユーザー取得
- Project一覧
- Project詳細
- Project作成
- Project編集
- Project削除

---

## 学習内容

- Logger
- Validation
- Exception
- DTO
- Mapper
- Spring Security(Session)
- Flyway
- MySQL
- Nuxt4 middleware
- Pinia auth store
- Zod Schema

---

## フロントエンド実装方針

v0.1 では、画面デザインを作り込みすぎず、Nuxt4 と Spring Boot API の接続、認証状態管理、Project CRUD の操作完了を優先する。

Tailwind CSS は UI/UX 設計が固まってから導入する。v0.1 前半では、素の Vue/Nuxt 実装で画面遷移、フォーム、API 通信、状態管理を理解する。

### 実装順序

1. ログアウト API を実装する
2. ログイン中ユーザー取得 API を実装する
3. Pinia で認証状態を管理する
4. Nuxt middleware で未ログイン時の遷移制御を行う
5. Zod Schema でログインフォームと Project フォームのバリデーションを行う
6. UI/UX 設計を整理する
7. Tailwind CSS を導入する
8. 共通UI部品を整備する

### 認証 API

```text
POST /api/auth/logout
GET  /api/auth/current-user
```

`POST /api/auth/logout` はサーバー側のセッションを破棄する。

`GET /api/auth/current-user` はログイン中ユーザー情報を返す。未ログインの場合は 403 を返す。

### フロントエンド認証状態管理

```text
stores/auth.ts
  user
  isLoggedIn
  fetchCurrentUser()
  login()
  logout()

middleware/auth.ts
  未ログインの場合は /login へ遷移する
```

### Tailwind CSS 導入方針

Tailwind CSS は、認証状態管理とフォームバリデーションの流れができた後に導入する。

導入時は、まず以下の最小範囲に限定する。

- 余白
- フォームの視認性
- ボタンの見分けやすさ
- 画面全体の最低限のレイアウト

### 共通UI部品の整備方針

v0.1 の仕上げとして、Project CRUD 画面で繰り返し使うUIを共通部品化する。

まずは画面の責務を軽くすることを目的とし、過度に汎用化しすぎない。

優先順序は以下とする。

1. ProjectTable
2. AppPagination
3. AppBreadcrumb
4. AppLoading
5. Toast基盤
6. AppBadge
7. AppConfirmDialog

各部品の責務は以下とする。

- ProjectTable: Project一覧のテーブル表示
- AppPagination: ページ移動操作
- AppBreadcrumb: 現在位置の表示
- AppLoading: API実行中の共通ローディング表示
- Toast基盤: 成功・失敗メッセージの一時表示
- AppBadge: 状態や分類の短いラベル表示
- AppConfirmDialog: 削除など重要操作の確認表示

Toast基盤は、単体コンポーネントだけでなく状態管理も含めて設計する。

```text
components/ui/AppToast.vue
composables/useToast.ts
```

---

## 完了条件

- 認証が動く
- Project CRUDが動く
- Dockerで起動できる
- APIテスト完了
- Nuxtから操作できる
- 初期ユーザーをFlywayで作成できる
- ログアウトできる
- ログイン中ユーザーを取得できる
- 未ログイン時に保護画面へ直接アクセスできない
- ログインフォームと Project フォームでフロント側バリデーションが動く
- Project一覧をテーブル形式で表示できる
- Project一覧でページネーションが動く
- Project CRUD画面の主要UIが共通部品化されている

---

## レビュー項目

- Controllerは薄いか
- Serviceに業務ロジックがあるか
- Transactionは適切か
- Loggerは適切か
- Validationは適切か
- 例外レスポンスはRFC7807方針に沿っているか
- 命名は適切か

---

## Definition of Done

- 全Issue完了
- PRレビュー完了
- 動作確認済み
- ドキュメント更新済み
- 学習ログ作成済み

---

## 振り返り

### 良かったこと

### 苦労したこと

### 学んだこと

### 次回改善すること
